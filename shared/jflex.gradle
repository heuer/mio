/**
 * JFlex task
 */

apply plugin: 'java'

task generateLexer << {
    ant.taskdef classname: 'jflex.anttask.JFlexTask', name: 'jflex', classpath: configurations.buildproject.asPath
    ant.jflex(file: 'src/grammar/' + project.ext.flexSource, destdir: 'src/main/java/')
}
